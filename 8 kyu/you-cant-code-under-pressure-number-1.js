// http://www.codewars.com/kata/you-cant-code-under-pressure-number-1/solutions/javascript/me

function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i * 2;
}

// alternative solution
function doubleInteger(i) {
  return i * 2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i *2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i*2;
}


// alternative solution
function doubleInteger(i) {

  return i * 2;
}

// alternative solution
function doubleInteger(i) {  
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i*2;
};

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  ;
return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return (i*2);
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i * 2;
}


// alternative solution
function doubleInteger(i) {
return i * 2;
}

// alternative solution
function doubleInteger(i) {
  
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return (i *2);
}

// alternative solution
function doubleInteger(i) {

  return i*2;

}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return (i * 2);
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i * 2
}


// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
return i * 2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
return i*2;
}

// alternative solution
function doubleInteger(i) {
return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  
  return (i*2);
}

// alternative solution
function doubleInteger(i) {
 
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  return i* 2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer.
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.  
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i* 2;
}

// alternative solution
function doubleInteger(i) {

  return i*2;
}

// alternative solution
function doubleInteger(i) {
   return i * 2;
}

// alternative solution
function doubleInteger(i) { 
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
   return i*2;
}

// alternative solution
function doubleInteger(i) { return i * 2 }

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i  * 2;
}

// alternative solution
function doubleInteger(i) {return i*2;}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i*2
}

// alternative solution
function doubleInteger(i) {
  
  return i * 2;
}

// alternative solution
function doubleInteger(i) {
  return i * 2;
}


// alternative solution
function doubleInteger(i) {
  return i *2; 
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i *2 ;
}

// alternative solution
function doubleInteger(i) {
  
  return(i * 2)
}

// alternative solution
function doubleInteger(i) {
    // i will be an integer. Double it and return it.
   
    return i * 2;
}


// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.

  return i*2;
}

// alternative solution
function doubleInteger(i) {
   return i*2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  
  return i * 2;
}

// alternative solution
function doubleInteger(i) {
  return i*2;
}



// alternative solution
function doubleInteger(i){
  return i * 2;
}

// alternative solution
function doubleInteger(i) {
  // KimSonia Negondeni
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  return i*2;

}

// alternative solution
function doubleInteger(i) {
// i will be an integer. Double it and return it.
return i * 2;
}

// alternative solution
function doubleInteger(i) {return i*2}

// alternative solution
function doubleInteger(i) {

return i*2;
}

// alternative solution
function doubleInteger(i) {
  return i *2 ;
}

// alternative solution
function doubleInteger(i) {  
  return i * 2;
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i * 2 ;
}

// alternative solution
 function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i*2 ;
}


// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return  i*2;
}

// alternative solution
function doubleInteger(i) {
  return (i*2);
}

// alternative solution
function doubleInteger(i) {
    return i * 2;
}

// alternative solution
function doubleInteger(i) {
 // i will be an integer. Double it and return it.
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  
  return i * 2;
}



// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return i
  return i * 2;
}

// alternative solution
function doubleInteger(i) {
  return i * 2
}

// alternative solution
function doubleInteger(i) 
{
  return i*2;
}

// alternative solution
function doubleInteger(i) {
  return i * 2; 
}

// alternative solution
function doubleInteger(i) {
 return (i*2);
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
  return i * 2
}

// alternative solution
function doubleInteger(i) {
  // i will be an integer. Double it and return it.
    return i*2;
}

// alternative solution
function doubleInteger(i) {
    return i*2;
}

// alternative solution
function doubleInteger(i) {
  return i *2
}

// alternative solution
  function doubleInteger(i) {
  
  return i * 2
  
}

// alternative solution
function doubleInteger(i) {
  return i*2
}