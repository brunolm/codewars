// http://www.codewars.com/kata/dna-to-rna-conversion/solutions/javascript/me

function DNAtoRNA(dna){
  return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,"U")
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U');
  // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, "U"); 
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U')
}

// alternative solution
function DNAtoRNA(dna) {
  return (dna.replace(/T/g,"U"));
}

// alternative solution
function DNAtoRNA(dna) {
 return dna.replace(/T/g, 'U');

}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g,'U');
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g,"U");
}

// alternative solution
function DNAtoRNA(dna) {
return dna.replace(/T/g, "U");
  // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
    return dna.replace(/T/g, "U")
};

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,'U')
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, "U");  
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,"U");
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, "U")
}

// alternative solution
function DNAtoRNA(dna) {
  return (dna.replace(/T/g, "U"));
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,'U');
}

// alternative solution
function DNAtoRNA(dna) {
    return dna.replace(/T/g,'U');
}

// alternative solution
function DNAtoRNA(dna) { 
  return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  //Use REGEX to replace all ocurrences of 'T', by 'U'
  return dna.replace(/T/g,'U');
}

// alternative solution
function DNAtoRNA(dna) {
  // use a regexp to replace all T characters in string with U
  return dna.replace(/T/g,'U');
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  
  return dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,"U")  
}

// alternative solution
function DNAtoRNA(dna) {
 return dna.replace(/T/g, "U");
  // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
    return dna.replace(/T/g, 'U')
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, "U")
}

// alternative solution
function DNAtoRNA(dna) {return dna.replace(/T/g, 'U');}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
 return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
 return dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U'); 
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, "U")// create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,'U');
  // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
    return dna.replace(/T/g,"U")
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace( /T/g, 'U' );
}

// alternative solution
function DNAtoRNA(dna) {
    return dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {return dna.replace(/T/g,"U")};

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence

  return dna.replace(/T/g, "U");;
}

// alternative solution
function DNAtoRNA(dna) {
return dna.replace(/T/g,'U');
  // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
  
  return dna.replace(/T/g, 'U');
  
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, "U");

}

// alternative solution
function DNAtoRNA(dna) {
 return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U');
}


// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,"U");
};

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,"U");
  
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, "U");
  
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  
return dna.replace(/T/g, "U");
 
}

// alternative solution
function DNAtoRNA(dna) {
  return  dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
 
 return  dna.replace(/T/g,"U");
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, 'U');  
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,"U"); 
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, 'U')
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
 return dna.replace(/T/g,'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/(T)/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g , 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
 return dna.replace(/T/g, 'U');
  
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g,'U')
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, "U");}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, "U");
 }

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace( /T/g, "U" );
}

// alternative solution
function DNAtoRNA(dna) 
{
  return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
   return dna.replace(/T/g,'U');
}

// alternative solution
function DNAtoRNA(dna) {

  return dna.replace(/T/g, "U");
  
}

// alternative solution
function DNAtoRNA(dna) {
    return dna.replace(/T/g,"U");
}

// alternative solution
function DNAtoRNA(dna) {
return dna.replace(/T/g,"U");
  // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, 'U')
      
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, 'U' );
}

// alternative solution
function DNAtoRNA(dna) {return dna.replace(/T/g,'U');}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g, "U");
  
  
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,'U');
  
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
  
  return dna.replace(/T/g,"U");
}

// alternative solution
function DNAtoRNA(dna) {
 return dna.replace(/T/g, 'U') 
}

// alternative solution
function DNAtoRNA(dna) {
   return dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g,"U") ;
}

// alternative solution
function DNAtoRNA(dna) {
 return dna.replace(/T/g,'U')
}

// alternative solution
function DNAtoRNA(dna) {
    return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U');
};

// alternative solution
function DNAtoRNA(dna) {
 return dna.replace(/T/g, "U");
 // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
return dna.replace(/T/g,"U");
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U');  
}

// alternative solution
function DNAtoRNA(dna) {
// create a function which returns an RNA sequence from the given DNA sequence
  return dna.replace(/T/g,"U")

}

// alternative solution
function DNAtoRNA(dna) {

return dna.replace(/T/g, "U")
  // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
   return dna.replace(/T/g, 'U')
}

// alternative solution
function DNAtoRNA(dna) {
  // create a function which returns an RNA sequence from the given DNA sequence
return dna.replace(/T/g, "U");
}


// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U');
  }

// alternative solution
function DNAtoRNA(dna) {
return dna.replace(/T/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
   return dna.replace(/T/g , "U");
}

// alternative solution
function DNAtoRNA(dna) {
return dna.replace(/T/g,'U');}

// alternative solution
function DNAtoRNA(dna) {
   return dna.replace(/T/g, 'U');
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/(T)/g, "U");
}

// alternative solution
function DNAtoRNA(dna) {
 return dna.replace(/T/g,"U") // create a function which returns an RNA sequence from the given DNA sequence
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace(/T/g, 'U');  
}

// alternative solution
function DNAtoRNA(dna) {
  return dna.replace( /T/g , 'U');
}