// http://www.codewars.com/kata/enumerable-magic-number-4-true-for-none/solutions/javascript/me

function none(arr, fun){
  return !arr.some(fun);
}

// alternative solution
function none(arr, fun){
  return !(arr.some(fun));
}

// alternative solution
function none(arr, fun){
    return !(arr.some(fun));
}

// alternative solution
function none(arr, fun){
  return !arr.some(fun)
}

// alternative solution
function none(arr, fun){
    return !arr.some(fun);
}

// alternative solution
function none(arr, fun){ return ! arr.some(fun); }

// alternative solution
function none(arr, fun){
  return ! arr.some(fun);
}

// alternative solution
function none(arr, fun) {
  return !arr.some(fun);
}

// alternative solution

function none(arr, fun){
  return !(arr.some(fun));
}

// alternative solution
function none(arr, fun){
  // ...
  return !arr.some(fun);
}

// alternative solution
function none(arr, fun){
  return !arr.some( fun )
}

// alternative solution
function none(arr, fun){
  return  !arr.some(fun);
}

// alternative solution
function none(arr, fun){
return !arr.some(fun)
}

// alternative solution
function none(arr, fun){
 return !arr.some(fun)
}

// alternative solution
function none(arr, fun){
  return !arr.some(fun);
}

// alternative solution
function none(arr, fun){
 return !arr.some(fun);
}

// alternative solution
function none(arr, fun) {
  return !arr.some(fun)
}

// alternative solution
function none(arr, fun){
   return !arr.some(fun);
}

// alternative solution
function none(arr, fun) {
    return !arr.some(fun);
}

// alternative solution
function none(arr, fun){
  return !arr.some(fun)
  // ...
}

// alternative solution
function none(arr, fun){
  return !arr.some( fun );
}

// alternative solution
function none(arr, fun){
  // ...
 
  
  return !arr.some(fun)
  
  
}

// alternative solution
function none(arr, fun){
  return !arr.some(fun);
  
  }

// alternative solution
function none(arr, fun) {
  // if fun returns true for 1 or more elements in array
  // some will return true, so negate that.
  return !arr.some(fun);
}

// alternative solution
function none(arr, fun){
  return !arr.some(fun);
}



// alternative solution
function none(arr, fun){
 return !arr.some(fun); 
}

// alternative solution
function none(arr, fun){
  // ...
return !arr.some(fun);
}

// alternative solution
function none(arr, fun){
 return !arr.some(fun);
  
}

// alternative solution
function none(arr, fun){
   return !arr.some(fun)
}

// alternative solution

  function none(arr, fun){
  return !arr.some(fun);
}


// alternative solution
function none(arr, fun){
  return !(arr.some(fun))
}