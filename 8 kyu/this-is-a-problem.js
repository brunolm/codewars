// http://www.codewars.com/kata/this-is-a-problem/solutions/javascript/me

function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name= this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + " " + this.lastName;
}



// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
    
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName ;
    //return {name: this.firstName + ' ' + this.lastName};
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}



// alternative solution
function NameMe(first, last) {
  this.firstName = first;
  this.lastName = last;
  this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    
    this.firstName = first;
    this.lastName = last;
    this.name =  this.firstName + ' ' + this.lastName;
    
    
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name= this.firstName + ' ' + this.lastName;
}


// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name=this.firstName+' '+this.lastName;
   // return this.name;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + " " + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name=  this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;    
    this.name = this.firstName + ' ' + this.lastName;
}



// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name =  this.firstName + ' ' + this.lastName; 
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name=this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName
}

// alternative solution
function NameMe(first, last) 
{
                this.firstName = first;
                this.lastName = last;
              this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;

}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + " " + this.lastName;
    }

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name =  this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
   this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name= this.firstName + ' ' + this.lastName
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name=this.firstName + ' ' + this.lastName;
};


// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName

}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName; 
}

// alternative solution
function NameMe(first, last) 
{
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
     
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName  = last;
    this.name      = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + " " + this.lastName;
    
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName+ ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
    //return {name: this.firstName + ' ' + this.lastName};
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + " " + this.lastName;
    //return {name: this.firstName + ' ' + this.lastName};
    
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;   
}

// alternative solution

function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name =  this.firstName + ' ' + this.lastName;
}


// alternative solution
function NameMe(first, last) {
   this.firstName = first;
   this.lastName = last;
   this.name = this.firstName + " " + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = (this.firstName + ' ' + this.lastName);
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    //return {name: this.firstName + ' ' + this.lastName};
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name  = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = (this.firstName + ' ' + this.lastName);
}


// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;    
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name =  this.firstName + " " + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
    //return {name: this.firstName + ' ' + this.lastName};
}



// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this. name =  this.firstName + ' ' + this.lastName;
}

// alternative solution



function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}


// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + " " + this.lastName;
    //return {name: this.firstName + ' ' + this.lastName};
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName +' '+ this.lastName;
}



// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + " " + this.lastName;
}


// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName+' '+this.lastName;
    //Don't return anything from a constructor
    //create name as a member attribute
    //return {name: this.firstName + ' ' + this.lastName};
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name=this.firstName + ' ' + this.lastName;
}


// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName ;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' +  this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName= first;
    this.lastName = last;
    this.name= this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name =this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + " " + this.lastName;
      
}

// alternative solution
function NameMe(first, last) {

    this.firstName = first;
    this.lastName = last;
    this.name= this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name     = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name=this.firstName + ' ' + this.lastName;

}

// alternative solution

function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
     this.name= this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    //return {name: this.firstName + ' ' + this.lastName};
    
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName
    }

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName+" "+this.lastName;
}

// alternative solution
function NameMe(first, last) {
  this.firstName = first;
  this.lastName = last;
  this.name = this.firstName + ' ' + this.lastName
    
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' '+ this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
     this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName+' '+this.lastName;
}

// alternative solution
function NameMe(first, last) {
  
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
    //return {name: self.firstName + ' ' + self.lastName};
}

// alternative solution
function NameMe(first, last) {

    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this. lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName +" " + this.lastName


}


// alternative solution
function NameMe(first, last) {
    this.firstName = first
    this.lastName = last
    this.name = this.firstName + ' ' + this.lastName
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
    
}


// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    //edited to remove return and assign firstName LastName concat to this.name
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
};

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
};


// alternative solution
function NameMe(first, last) {
   this.firstName = first;
   this.lastName = last;
   this.name = this.firstName + ' ' + this.lastName
}

// alternative solution


function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name =  this.firstName + ' ' + this.lastName;
    
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    //return {name: this.firstName + ' ' + this.lastName};
    this.name = this.firstName + ' ' + this.lastName
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
      
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;    
    this.name = this.firstName +" "+this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name  = this.firstName + ' ' + this.lastName
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;

    this.name = this.firstName + " " + this.lastName;
    
}

// alternative solution
function NameMe(first, last) {
  this.firstName = first;
  this.lastName = last;
  this.name = this.firstName + ' ' + this.lastName;
}


// alternative solution
function NameMe(first, last) {
   this.firstName = first;
   this.lastName = last;
   this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName= last;
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    
    this.name = this.firstName + ' ' + this.lastName ;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}
 

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
 
}

// alternative solution
function NameMe(first, last) {
  this.firstName = first;
  this.lastName = last;
  this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
   this.name=this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = 
      this.firstName + ' ' + this.lastName;
};

// alternative solution
function NameMe (first, last) {
    this.firstName = first
    this.lastName = last
    this.name = this.firstName + ' ' + this.lastName
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
    
    
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    
    this.name = this.firstName + " " + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = (this.firstName + " " + this.lastName);
}

// alternative solution
function NameMe(first, last) {
  this.firstName = first;
  this.lastName  = last;
  this.name = this.firstName + ' ' + this.lastName;
}


// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName +" "+ this.lastName;
}

// alternative solution

function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name = this.firstName + ' ' + this.lastName;
}


// alternative solution
function NameMe(first, last) {
    this.firstName = first,
    this.lastName = last,
    this.name = this.firstName + ' ' + this.lastName;
}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
this.name = this.firstName  + ' ' + this.lastName ;

}

// alternative solution
function NameMe(first, last) {
    this.firstName = first;
    this.lastName = last;
    this.name= this.firstName+' '+this.lastName
};

// alternative solution
function NameMe(first, last) {
      this.firstName =  first
      this.lastName = last
      this.name = this.firstName + ' ' + this.lastName
}

// alternative solution
function NameMe(first, last) {
  this.firstName = first;
  this.lastName = last;
  this.name = this.firstName + " " + this.lastName;
}