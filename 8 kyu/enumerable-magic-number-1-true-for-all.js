// http://www.codewars.com/kata/enumerable-magic-number-1-true-for-all/solutions/javascript/me

function all( arr, fun ){
  return arr.every(fun)
}

// alternative solution
function all( arr, fun ){
  return arr.every(fun);
}

// alternative solution
function all(arr,fun) {
  return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
  // ...
  return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
 return arr.every(fun)
}

// alternative solution
function all(arr, fun) {
  return arr.every(fun);
}

// alternative solution
function all(arr, fun) {
  return arr.every(fun)
}

// alternative solution
function all( arr, fun ){
 return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
    return arr.every(fun);
}

// alternative solution
function all(arr, fun){
  return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
  return arr.every(fun); 
}

// alternative solution
function all( arr, fun ){
  return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
  //use every method to loop through each item in the array; with every, if each item passes the function, it returns true
 return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
  return arr.every(fun);
  // ...
}

// alternative solution
function all( arr, fun ){
  // ...
  return arr.every(fun)
}

// alternative solution
function all( arr, fun )
{
   return arr.every(fun);
}

// alternative solution
function all(arr, fun) {
  return(arr.every(fun));
}

// alternative solution
function all( arr, fun ){
     return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
  return arr.every(fun);
  
}

// alternative solution
function all(arr, fun) {
    return arr.every(fun);
}

// alternative solution
function all( arr, fun ){
  return arr.every(fun);
  // ...
}

// alternative solution
function all( arr, fun ){
return arr.every(fun)
}

// alternative solution
function all( arr, fun ){
  return arr.every( fun );
}

// alternative solution
function all(arr, fun){
  return arr.every(fun)
}

// alternative solution
function all( arr, fun ){
  // ...
return arr.every(fun)
}



// alternative solution
function all( arr, fun ){
  // for empty array return true
  return arr.every(fun);
}