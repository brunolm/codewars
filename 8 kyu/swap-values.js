// http://www.codewars.com/kata/swap-values/solutions/javascript/me

function swapValues(args) {
  var temp = args[0];
  args[0] = args[1];
  args[1] = temp;
}

// alternative solution
function swapValues(args) {
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
   // var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
    
}

// alternative solution
function swapValues(args) {
    // var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {    
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
 //   var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
   
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {

    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arguments[0]);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.splice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;    
}

// alternative solution
function swapValues(args) {
    //var args = arguments; //Array.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {   
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {

    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
    
}

// alternative solution
function swapValues(args) {        
    var temp = args[0];            
    args[0] = args[1];
    args[1] = temp;
    
}

// alternative solution
function swapValues(args) {  
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}



// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;   

}

// alternative solution
function swapValues(args) {
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;

}

// alternative solution
function swapValues(args) 
{
    //var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
  var temp = args[0];
  args[0] = args[1];
  args[1] = temp;
}


// alternative solution
var args = Array.prototype.slice.call(arguments);

function swapValues(args) {
    
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
   
}

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
    
}

// alternative solution
function swapValues(args){
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
    //return args;
}

// alternative solution
function swapValues(args) {
     var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arr);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
};

// alternative solution
function swapValues(args) {
    var temp = args[0];
       
    args[0] = args[1];
       
    args[1] = temp;
   
}

// alternative solution
function swapValues(args) {
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}


// alternative solution
function swapValues(args) {
    var temp = args[0];    
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arguments); // wtf?
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
    
    
}

// alternative solution
function swapValues(args) {
  
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
   
    var temp = args[0];
   args[0] = args[1];
    args[1] = temp;
   }

// alternative solution
function swapValues(args) {
    //var args = Array.prototype.slice.call(arguments);
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
    //arguments = args;
}

// alternative solution

function swapValues(args) {
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}

// alternative solution
function swapValues(args) {
    var temp = args[0];
    
    args[0] = args[1];
    args[1] = temp;
}