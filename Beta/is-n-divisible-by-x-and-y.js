// http://www.codewars.com/kata/is-n-divisible-by-x-and-y/solutions/javascript/me

function isDivisible(n, x, y) {
  return !(n % x) && !(n % y);
}

// alternative solution
function isDivisible(n, x, y) {
  return !(n%x) && !(n%y);
}

// alternative solution
function isDivisible(n, x, y){
  return !(n % x) && !(n % y);
}

// alternative solution
function isDivisible(n, x, y) {
  return (!(n % x) && !(n % y));
}

// alternative solution
function isDivisible(n, x, y) {
    return !(n % x) && !(n % y);
}

// alternative solution
function isDivisible(n, x, y) {
   return !(n % x) && !(n % y);
}

// alternative solution
function isDivisible(n, x, y) {
  return !(n % x) && !(n % y)
}

// alternative solution
function isDivisible(n, x, y) {
  return !(n % x) && !(n % y)
}

// alternative solution
function isDivisible(n, x, y) {
  return !(n%x)&&!(n%y);
}

// alternative solution
function isDivisible(n, x, y) {
  return (!(n%x)&&!(n%y));
}

// alternative solution
function isDivisible(n, x, y) {
  return !(n%x)&&!(n%y);
}

// alternative solution
function isDivisible(n, x, y) {
  return (!(n%x) && !(n%y));
}

// alternative solution
function isDivisible(n, x, y) {
return !(n%x) && !(n%y);
}

// alternative solution
function isDivisible(n, x, y) {
  return !(n%x) && !(n%y)
}