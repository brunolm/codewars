// http://www.codewars.com/kata/testing-1-2-3/solutions/javascript/me

var number=function(array){
  return array.map((x, i) => `${i+1}: ${x}`);
}

// alternative solution
var number=function(array){
  return array.map((x, i) => `${i + 1}: ${x}`);
}

// alternative solution
var number=function(array){
  return array.map((x,i) => `${i+1}: ${x}`);
}