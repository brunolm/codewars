// http://www.codewars.com/kata/say-hello/solutions/javascript/me

function greet(name){
  if(!name) return null;
  return `hello ${name}!`;
}

// alternative solution
function greet(name) {
if (!name) return null;
return `hello ${name}!`;
}

// alternative solution
function greet(name) {
  if(!name) return null;
  return `hello ${name}!`;
}

// alternative solution
function greet(name) {
  if (!name) return null;
  return `hello ${name}!`
}

// alternative solution
function greet (name) {
  if(!name) return null;
  
  return `hello ${name}!`;
}