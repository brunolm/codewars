// http://www.codewars.com/kata/binary-addition/solutions/javascript/me

const addBinary = (a, b) => (a + b).toString(2);

// alternative solution
const addBinary = (a,b) => (a + b).toString(2);

// alternative solution
const addBinary = (a, b) => (a + b).toString(2)

// alternative solution
const addBinary = (a,b) => (a+b).toString(2);

// alternative solution
const addBinary = (a,b) => (a+b).toString(2)

// alternative solution
const addBinary = (a, b) => 
  (a + b).toString(2);